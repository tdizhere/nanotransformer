[project]
name = "decoder-only-language-model"
version = "0.1.0"
description = "Character-level bigram and GPT language models trained on Shakespeare plays"
requires-python = ">=3.9"
dependencies = [
    "torch>=2.0.0",
    "kagglehub>=0.3.0",
]

[tool.uv]
package = false
